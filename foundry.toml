[profile.default]
src = 'src'                                                   # the source directory
test = 'test'                                                 # the test directory
out = 'out'                                                   # the output directory (for artifacts)
libs = ['lib']                                                # a list of library directories
gas_reports = ["*"]

[fuzz]
runs = 256                                                    # change me to 10,000 for full testing - takes 3 hours lol